import React from 'react'
import { AppBar, Toolbar } from '@mui/material';

export const Header = () => {

    return <AppBar position="sticky" color="secondary">
        <Toolbar>
            Header
        </Toolbar>
    </AppBar>
}